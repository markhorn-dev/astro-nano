---
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
  {
    date.toLocaleDateString("ZH-CN", {
      month: "2-digit",
      day: "2-digit",
      year: "numeric",

    })
  }
</time>
